<Window x:Class="Prism_Demo.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prism_Demo"
        mc:Ignorable="d"
        xmlns:prism="http://www.codeplex.com/prism"
        Title="Modular App" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="80*"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Column="0" Name="MenuControl" prism:RegionManager.RegionName="MenuRegion"/>
        <DockPanel Grid.Column="1" LastChildFill="True">
            <ContentControl DockPanel.Dock="Top" Name="NavControl" prism:RegionManager.RegionName="NavRegion"/>
            <ContentControl Name="RegionControl" prism:RegionManager.RegionName="ContentRegion"/>
        </DockPanel>
    </Grid>
</Window>
